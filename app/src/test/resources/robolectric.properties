# Robolectric instantiates this Application for every unit test instead of the manifest's
# StopcastApp, so the suite skips standing up the on-device file sink and the process-wide
# crash handler each test would otherwise install. See app.stopcast.TestStopcastApp.
application=app.stopcast.TestStopcastApp
