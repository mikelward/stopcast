# Robolectric instantiates this Application for every unit test instead of the manifest's
# StopdashApp, so the suite skips standing up the on-device file sink and the process-wide
# crash handler each test would otherwise install. See app.stopdash.TestStopdashApp.
application=app.stopdash.TestStopdashApp
