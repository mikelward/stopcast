<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Precise location for the nearby-stops search: a coarse fix put the nearest stop up to
         ~1 km off (a stop half a mile away read as nearest), so stopcast requests FINE and uses
         it when granted (maintainer, 2026-09-19). COARSE stays declared so the runtime dialog
         offers the precise/approximate choice and an approximate grant still finds stops,
         degraded. Location is used on demand only — sent to TfL to find stops, never on a
         background refresh (SPEC Finding stops / Privacy). -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- StopCast shows no ads: strip the advertising-ID and ad-attribution permissions the Firebase
         Analytics dependency would otherwise merge in. -->
    <uses-permission
        android:name="com.google.android.gms.permission.AD_ID"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.ACCESS_ADSERVICES_AD_ID"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.ACCESS_ADSERVICES_ATTRIBUTION"
        tools:node="remove" />

    <application
        android:name=".StopcastApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.StopCast">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.StopCast">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- The home-screen (and, where the OS allows, lock-screen) widget. Glance's receiver
             handles the APPWIDGET_UPDATE plumbing; the provider meta-data points at the sizing
             and category config. exported=true because the launcher/host binds it. -->
        <receiver
            android:name=".widget.StopCastWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/stopcast_widget_info" />
        </receiver>

        <!-- Serves the bug-report screenshot to the app the user picks in the share sheet.
             Not exported: only the chooser target we grant a read to (per-Uri, per-share) can
             read it; nothing else, and never the rest of the cache (see @xml/file_paths). -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Telemetry never starts on its own: collection stays off until StopcastApp applies the
             persisted "Help make StopCast better" choice, and both SDKs then remember the applied
             value across process starts. Inert in builds without a Firebase config
             (dev-docs/firebase.md). -->
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="google_analytics_default_allow_ad_personalization_signals"
            android:value="false" />
        <meta-data
            android:name="google_analytics_adid_collection_enabled"
            android:value="false" />

        <!-- Republishes the widget's snapshot when a watch gains the app or comes back in reach
             (dev-docs/wear-os.md). -->
        <service
            android:name=".watch.PhoneWearListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.CAPABILITY_CHANGED" />
                <data
                    android:host="*"
                    android:pathPrefix="/stopcast_watch"
                    android:scheme="wear" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data
                    android:host="*"
                    android:pathPrefix="/stopcast/refresh"
                    android:scheme="wear" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data
                    android:host="*"
                    android:pathPrefix="/stopcast/complication-rows"
                    android:scheme="wear" />
            </intent-filter>
        </service>
    </application>

</manifest>
