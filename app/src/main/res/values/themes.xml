<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws everything (see StopCastTheme); this platform theme only
         styles the launch window. Framework theme, so no
         com.google.android.material dependency is pulled in. -->
    <style name="Theme.StopCast" parent="android:Theme.Material.NoActionBar" />
</resources>
